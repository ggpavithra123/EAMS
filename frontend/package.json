{
  "name": "eams-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^1.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.1",
    "react-scripts": "5.0.1",
    "react-toastify": "^9.1.3"
  },
  "overrides": {
    "jsesc": "3.0.2"
  },
  "engines": {
    "node": ">=18 <22"
  },
  "scripts": {
    "prestart": "node -e \"const m=Number(process.versions.node.split('.')[0]);if(m>=22){console.warn('Warning: Node 22 may be unstable with react-scripts 5. Prefer Node 20 if available.')}\"",
    "start": "react-scripts start",
    "build": "node node_modules/react-scripts/bin/react-scripts.js build"
  },
  "proxy": "https://eams-zfwj.onrender.com",
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
